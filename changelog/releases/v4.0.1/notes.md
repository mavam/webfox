Webfox now presents active pi extension progress with a neutral muted glyph instead of a warning color. This release also aligns published schema links with the current package version.

## 🐞 Bug fixes

### Neutral progress glyph color

The pi extension now renders the active progress glyph in the muted color instead of the warning color, keeping in-progress web requests visually neutral.

*By @mavam.*
